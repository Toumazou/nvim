neovim first reads the nvim-lsp default configuration 
and then reads my custom configuration saved in after directory, overwriting the defaults

now I don't have any custom configs but have set up this for the future 
